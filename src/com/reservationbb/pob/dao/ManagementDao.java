package com.reservationbb.pob.dao;

import com.reservationbb.pob.model.Management;

public class ManagementDao {

	Management mng = new Management();

/*	public int ganaciaPorClient() {
		int numPersons = mng.getNumberOfPersons();
		double pricePerNight = mng.getPricePerNight();
		double ganancia;
		if ((numPersons > 0) && (numPersons < 3)) {
			if (pricePerNight  > 0) {
				//numero de noches;
				ganancia = pricePerNight * 
			}
		}

		return 0;

	}*/

}
